import formulas

def convertMuriMatoToHectares(toConvert):
	return float(toConvert) * formulas.muriMatoToHectares 
	
def convertHectaresToMuriMato(toConvert):
	return float(toConvert) / formulas.muriMatoToHectares 
	
def convertPathiMatoToHectares(toConvert):
	return float(toConvert) * formulas.pathiMatoToHectares 
	
def convertHectaresToPathiMato(toConvert):
	return float(toConvert) / formulas.pathiMatoToHectares 
	
def convertRopaniToHectares(toConvert):
	return float(toConvert) * formulas.ropaniToHectares 
	
def convertHectaresToRopani(toConvert):
	return float(toConvert) / formulas.ropaniToHectares 
		
def convertBighaToHectares(toConvert):
	return float(toConvert) * formulas.bighaToHectares 
	
def convertHectaresToBigha(toConvert):
	return float(toConvert) / formulas.bighaToHectares
		
def convertAnnaToHectares(toConvert):
	return float(toConvert) * formulas.annaToHectares 
	
def convertHectaresToAnna(toConvert):
	return float(toConvert) / formulas.annaToHectares 
	
def convertDhurToHectares(toConvert):
	return float(toConvert) * formulas.dhurToHectares 
	
def convertHectaresToDhur(toConvert):
	return float(toConvert) / formulas.dhurToHectares
	
def convertKathaToHectares(toConvert):
	return float(toConvert) * formulas.kathaToHectares 
	
def convertHectaresToKatha(toConvert):
	return float(toConvert) / formulas.kathaToHectares
	
def convertMuriMatoToPathiMato(toConvert):
	return float(toConvert) * formulas.muriMatoToPathiMato 
	
def convertPathiMatoToMuriMato(toConvert):
	return float(toConvert) / formulas.muriMatoToPathiMato

def convertPathiMatoToManaMato(toConvert):
	return float(toConvert) * formulas.pathiMatoToManaMato
	
def convertManaMatoToPathiMato(toConvert):
	return float(toConvert) / formulas.pathiMatoToManaMato

def convertRopaniToAnna(toConvert):
	return float(toConvert) * formulas.ropaniToAnna 
	
def convertAnnaToRopani(toConvert):
	return float(toConvert) / formulas.ropaniToAnna
	
def convertBighaToKatha(toConvert):
	return float(toConvert) * formulas.bighaToKatha
	
def convertKathaToBigha(toConvert):
	return float(toConvert) / formulas.bighaToKatha
	
def convertKathaToDhur(toConvert):
	return float(toConvert) * formulas.kathaToDhur 
	
def convertDhurToKatha(toConvert):
	return float(toConvert) / formulas.kathaToDhur
